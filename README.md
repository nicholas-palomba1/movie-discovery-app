# React + TypeScript + Vite

For this project, I used the TMDB Discover Movie GET API request as well as Axios as a dependency.

To set up, I recommend adding the API Key to a .env.local file so that the apiToken external variable can be properly added to the getMovies.ts config object. 

```
